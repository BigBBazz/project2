<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/new_note.css">
</head>
<body>

    <section>
        <% for (const element of notes.learnarray)  { %>

            <p class = "mainLearning"><%= element %></p>

                <div class = "trunk">
                    <form action="/notes/learning_name/edit<%= notes.learning_id%>" method="get">
                        <input type="hidden" name="user_id" value="<%= notes.user_id%>" />
                        <input type="hidden" name="learning_id" value="<%= notes.learning_id%>" />
                        <input type="hidden" name="learning_name" value="<%= element %>" >
                        <button class = "style1">Current notes</button>
                    </form>
                </div>

                <div class = "trunk">
                    <form action="/notes/learning_name<%= notes.learning_id%>" method="get">
                        <input type="hidden" name="user_id" value="<%= notes.user_id%>" />
                        <input type="hidden" name="learning_id" value="<%= notes.learning_id%>" />
                        <input type="hidden" name="learning_name" value="<%= element %>" >
                        <button class = "style1">New note</button>
                    </form>
                </div>

        <% }%>
    </section>
    
</body>
</html>